Run project (root directory): npm run dev 
npm install in both directories
Currently using concurrently for frontend + backend

Vitest (frontend) <br>
run vitest: npm run test, npx vitest <br>

view vitest through browser ui: npm run test:ui <br>

generate a coverage report in frontend/coverage: npm test -- --coverage, npx vitest run --coverage